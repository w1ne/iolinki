CONFIG_IOLINKI=y
# Virtual PHY: builds on native_sim and connects to the virtual-master PTY via
# the IOLINK_PORT env var. For real hardware (e.g. nucleo) build with
# -DCONFIG_IOLINK_PHY_UART=y and an 'iolink_uart' devicetree alias instead.
CONFIG_IOLINK_PHY_VIRTUAL=y

CONFIG_LOG=y
