cmake_minimum_required(VERSION 3.10)
project(freertos_app C)

# Note: This is a skeleton. Requires FreeRTOS kernel sources to build.
# add_executable(freertos_app main.c)
# target_link_libraries(freertos_app PRIVATE iolinki)
